# This is the production python requirement's
name: cen16
channels:
  - conda-forge
dependencies:
  - python=3.10
  - autopep8
  - setuptools >= 21.0.0
  # general dev
  - flask
  - protobuf
  - openssl
  - yaml
  - python-dateutil
  # cloud
  - google-cloud-bigquery
  - yaml-cpp
  # config management
  - mamba
prefix: /home/jferreira/mambaforge
