# This is the production python requirement's
name: cen16-prod
channels:
  - conda-forge
dependencies:
  - python=3.10
  - autopep8
  - setuptools >= 21.0.0
  # general dev
  - flask
  - protobuf
  - openssl
  - yaml
  - python-dateutil
  # cloud
  - google-cloud-bigquery
  - yaml-cpp
  # config management
  - mamba
  # stuff
  - google-cloud-bigquery
  - prometheus_client
  - grpcio
  - grpcio-tools
  - python-kubernetes
  - pytest
  - mock
prefix: /home/jferreira/mambaforge
