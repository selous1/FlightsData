---
concurrency: 4
base: "http://localhost:5000"
iterations: 100000
rampup: 1

plan:
  - name: Test
    request:
      url: /

  - name: Test2
    request:
      url: /hello
      method: GET
# drill -b .config/tests/example.yaml -n -s
