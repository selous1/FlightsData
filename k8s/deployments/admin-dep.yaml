apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-d
  labels:
    app: admin
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin
  template:
    metadata:
      labels:
        app: admin
    spec:
      containers:
        - name: admin
          image: selous1/admin:v1.1
          ports:
            - containerPort: 5000
          env:
            - name: API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: my-secret
                  key: API_TOKEN
            - name: TABLE_NAME
              valueFrom:
                configMapKeyRef:
                  name: my-configmap
                  key: table 
---
apiVersion: v1
kind: Service
metadata:
  name: admin-s
  labels:
    app: admin
spec:
  type: NodePort
  selector:
    app: admin
  #clusterIP: None
  ports:
    - name: http
      protocol: TCP
      port: 80
      nodePort: 30005
      targetPort: 5000
