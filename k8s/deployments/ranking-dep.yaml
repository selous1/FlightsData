apiVersion: apps/v1
kind: Deployment
metadata:
  name: ranking-d
  labels:
    app: ranking
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ranking
  template:
    metadata:
      labels:
        app: ranking
    spec:
      containers:
        - name: ranking
          image: selous1/ranking:v1.5
          ports:
            - containerPort: 5004
          env:
            - name: API_TOKEN
              valueFrom:
                secretKeyRef:
                  name: my-secret
                  key: API_TOKEN
            - name: TABLE_NAME
              valueFrom:
                configMapKeyRef:
                  name: my-configmap
                  key: table
---
apiVersion: v1
kind: Service
metadata:
  name: ranking-s
  labels:
    app: ranking
spec:
  selector:
    app: ranking
  #clusterIP: None
  type: NodePort
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 5004
      nodePort: 30004